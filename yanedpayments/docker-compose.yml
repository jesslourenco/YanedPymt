services:
  db:
    container_name: postgres
    image: postgres
    ports:
      - "5332:5432"
    environment:
      POSTGRES_USER: jess
      POSTGRES_PASSWORD: password
      PGDATA: /data/postgres
    volumes:
      - db:/data/postgres
    networks:
      - db
    restart: unless-stopped

networks:
  db:
    driver: bridge

volumes:
  db:








